# Platform configuration for this application
# Generated by Backstage scaffolder
# DO NOT edit manually - managed via GitOps

app:
  name: test-approval
  namespace: test-approval
  description: test-approval
  owner: group:default/test-approval

build:
  arch: arm64
  platforms: linux/arm64
  ecrRepository: idp-test-approval
  tagStrategy: sha

deployment:
  replicas: 2
  size: medium
  exposePublic: true
  scheduling:

    nodeSelector:
      kubernetes.io/arch: arm64


platform:
  cluster: platform-eks
  region: us-east-1
  domain: timedevops.click
